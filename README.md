# MerchantGame (Хитрый Купец)

«Хитрый купец» Два купца отправились торговать за море. Каждый из них 
повез по N одинаковых тюков с товаром. В пути корабль попал в шторм и дал 
течь. Чтобы корабль не затонул, капитан приказал поднять весь груз на палубу 
корабля, расставить его вдоль бортов по периметру и выбросить за борт 
половину груза. 

## Описание
Программа имитирует классическую карточную игру, где колода из 10 карт делится между двумя игроками. Игра продолжается до тех пор, пока один из игроков не заберет все карты, либо пока игра не попадет в состояние «ботвы» (бесконечного цикла).
Использовать циклический однонаправленный список. 

## Груз выбрасывается по следующему правилу:  
  • каждый тюк получает порядковый номер, начиная с тюка, стоящего на 
  носу корабля;  
  • их нумерация осуществляется по часовой стрелке;  
  • номер первого выброшенного тюка соответствует текущему числу 
  месяца M;  
  • через K тюков следующий тюк с товаром выбрасывается. Один из 
  купцов прознал про это правило. Как необходимо расставить ему груз, 
  чтобы ни один из его тюков не был выброшен?

## Структура проекта
* main.py — точка входа в программу и пользовательский интерфейс.
* node.py – базовый элемент структуры данных (класс узла списка).
* circular_list.py – ядро системы: алгоритм исключения, расчёт безопасной позиции, логика визуализации.
* interface.py – слой взаимодействия с пользователем, валидация ввода, управление меню.

## Как запустить

### Требования
* Python 3.7 или выше
* Внешние библиотеки не требуются — используются только стандартные модули

### Запуск игры
1. Откройте терминал (командную строку)
2. Перейдите в папку с проектом:
   
''' bash
   cd путь/к/папке/проекта
3. Запустите игру командой:
   
bash
   python main.py
